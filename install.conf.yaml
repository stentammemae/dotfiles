- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc:
    ~/.config/nvim:
    ~/.gitconfig:
    ~/.zshrc:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
